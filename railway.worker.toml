[build]
builder = "NIXPACKS"

[deploy]
startCommand = "PLAYWRIGHT_BROWSERS_PATH=/app/.cache/ms-playwright DJANGO_SETTINGS_MODULE=config.settings.production celery -A config worker -l info --concurrency=1"
restartPolicyType = "ON_FAILURE"
